# ECDSA-Signature-Verifier-Bitcoin-secp256k1-Example
This Python script demonstrates how to **verify multiple ECDSA signatures** (using Bitcoin’s `secp256k1` curve) and analyze their components:   `r`, `s`, `z` (message hash), and whether they meet the **Low-S** rule — an important normalization condition in Bitcoin signatures.
